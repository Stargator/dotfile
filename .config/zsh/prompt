# Git Version Control System integration.
autoload -Uz vcs_info
zstyle ':vcs_info:*' enable git
zstyle ':vcs_info:*' formats ' %s' ' %b'
zstyle ':vcs_info:*' actionformats ' %s' ' %b'
precmd () { vcs_info }

# Prompt appearance.
setopt PROMPT_SUBST # Needed for dynamic prompt substitution.
PROMPT='
%B%F{magenta}%n%f @ %F{magenta}%m%f on %F{yellow}%y%f in %F{green}%~%f%F{green}${vcs_info_msg_0_}%f${vcs_info_msg_1_}
%1~ ▶ %b' # (Bold) User @ Host on Line \n Working Directory ▶
RPROMPT='%D{%H:%M:%S %A}' # Hour:Minute:Second Weekday
